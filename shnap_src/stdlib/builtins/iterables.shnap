iterableStr = $ arr {
    state = "["
    iter = arr.iterator()
    for iter {
        state += it
        if iter.hasNext() {
            state += ", "
        }
    }
    return: state + "]"
}

iterator = @ hasNext, next {}